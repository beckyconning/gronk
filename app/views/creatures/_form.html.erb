<%= form_for(@creature) do |f| %>
  <% if @creature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@creature.errors.count, "error") %> prohibited this creature from being saved:</h2>

      <ul>
      <% @creature.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.text_field :person_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :creature_definition_id %><br />
    <%= f.text_field :creature_definition_id %>
  </div>
  <div class="field">
    <%= f.label :dream_points %><br />
    <%= f.text_field :dream_points %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
