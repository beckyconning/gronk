<%= form_for(@dream_definition) do |f| %>
  <% if @dream_definition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dream_definition.errors.count, "error") %> prohibited this dream_definition from being saved:</h2>

      <ul>
      <% @dream_definition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desctiption %><br />
    <%= f.text_area :desctiption %>
  </div>
  <div class="field">
    <%= f.label :graphic_id %><br />
    <%= f.text_field :graphic_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
