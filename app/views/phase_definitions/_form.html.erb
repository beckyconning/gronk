<%= form_for(@phase_definition) do |f| %>
  <% if @phase_definition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phase_definition.errors.count, "error") %> prohibited this phase_definition from being saved:</h2>

      <ul>
      <% @phase_definition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br />
    <%= f.time_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br />
    <%= f.time_select :end_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
